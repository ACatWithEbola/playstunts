import {applyOriginalMaterialPattern,type OriginalMaterialPatterns} from './original-material-pattern.ts';
import * as THREE from 'three';
import type { Shape } from './types';
/** Preserve source face sidedness: reverse-facing underbody panels must not cover the body.
 * Use GPU facing at upgraded resolution; flag bit 0 retains genuinely two-sided panels. */
export function createCarModel(shape:Shape,color:number,originalPaint?:{paint:number;indices:readonly number[];palette:readonly number[]}&OriginalMaterialPatterns){const group=new THREE.Group();const originalColor=(material:number)=>{const index=originalPaint!.indices[material],rgb=originalPaint!.palette;return (rgb[index*3]<<16)|(rgb[index*3+1]<<8)|rgb[index*3+2];};const points=shape.vertices.map(v=>new THREE.Vector3(v[0]/400,v[1]/400,v[2]/400));for(const p of shape.primitives){if(p.type>=3&&p.type<=10){const positions:number[]=[];for(let i=1;i<p.indices.length-1;i++)for(const n of [p.indices[0],p.indices[i],p.indices[i+1]])positions.push(...points[n].toArray());const g=new THREE.BufferGeometry();g.setAttribute('position',new THREE.Float32BufferAttribute(positions,3));g.computeVertexNormals();const m=p.materials[0];let c=color;if(m===0||m===38||m===44)c=0x101720;else if([7,15,18,96,97].includes(m))c=0xd1d6d9;else if([16,17,40,41,42,43,76,77,78,79].includes(m))c=0x223d51;else if([4,12,45,46,47,127].includes(m))c=0xe34432;if(originalPaint){const index=originalPaint.indices[p.materials[originalPaint.paint]],rgb=originalPaint.palette;c=(rgb[index*3]<<16)|(rgb[index*3+1]<<8)|rgb[index*3+2];}const material=originalPaint?new THREE.MeshBasicMaterial({color:c,side:(p.flags&1)?THREE.DoubleSide:THREE.FrontSide,toneMapped:false}):new THREE.MeshStandardMaterial({color:c,metalness:.25,roughness:.43,side:THREE.DoubleSide,flatShading:true});if(originalPaint&&material instanceof THREE.MeshBasicMaterial)applyOriginalMaterialPattern(material,g,Array(positions.length/3).fill(p.materials[originalPaint.paint]),originalPaint);const node=new THREE.Mesh(g,material);node.castShadow=true;node.receiveShadow=true;node.userData.originalBodyFace=true;node.userData.originalPrimitive=shape.primitives.indexOf(p);group.add(node)}else if(originalPaint&&p.type===2){const geometry=new THREE.BufferGeometry().setFromPoints(p.indices.map(i=>points[i]));group.add(new THREE.LineSegments(geometry,new THREE.LineBasicMaterial({color:originalColor(p.materials[originalPaint.paint]),toneMapped:false})));}else if(originalPaint&&p.type===11){const center=points[p.indices[0]],radius=center.distanceTo(points[p.indices[1]]),sphere=new THREE.Mesh(new THREE.SphereGeometry(radius,16,12),new THREE.MeshBasicMaterial({color:originalColor(p.materials[originalPaint.paint]),toneMapped:false}));sphere.position.copy(center);group.add(sphere);}else if(p.type===12){const a=points[p.indices[0]],b=points[p.indices[3]],r=a.distanceTo(points[p.indices[1]]);const g=new THREE.CylinderGeometry(r,r,a.distanceTo(b),24);const wheel=new THREE.Mesh(g,originalPaint?[new THREE.MeshBasicMaterial({color:originalColor(p.materials[originalPaint.paint]),toneMapped:false}),new THREE.MeshBasicMaterial({color:originalColor(p.materials[originalPaint.paint]+1),toneMapped:false}),new THREE.MeshBasicMaterial({color:originalColor(p.materials[originalPaint.paint]+1),toneMapped:false})]:new THREE.MeshStandardMaterial({color:0x101318,roughness:.95}));wheel.position.copy(a).add(b).multiplyScalar(.5);wheel.quaternion.setFromUnitVectors(new THREE.Vector3(0,1,0),b.clone().sub(a).normalize());wheel.castShadow=true;group.add(wheel);const hub=new THREE.Mesh(new THREE.CylinderGeometry(r*(originalPaint?0x2500/16384:.56),r*(originalPaint?0x2500/16384:.56),a.distanceTo(b)+.012,12),originalPaint?new THREE.MeshBasicMaterial({color:originalColor(p.materials[originalPaint.paint]+2),toneMapped:false}):new THREE.MeshStandardMaterial({color:0x9fadb7,metalness:.7,roughness:.3}));hub.position.copy(wheel.position);hub.quaternion.copy(wheel.quaternion);group.add(hub)}}return group}
